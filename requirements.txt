# Runtime dependencies for the agent framework.
# Mirrors [project.dependencies] in pyproject.toml — keep both in sync.
# Install for development instead:  pip install -e .[dev]

numpy>=1.24
networkx>=3.1
pydantic>=2.5
httpx>=0.24
openai>=1.0
